version: '3.8'
services:
  agent-orchestrate:
    image: agentorchestrate:latest
    ports:
      - '8000:8000'
    volumes:
      - .cache:/app/cache
    environment:
      DEBUG: 'true'
      WORKER_COUNT: '16'